package com.aura.engine.event;

import com.aura.base.event.AbstractEvent;
import com.aura.engine.univers.EntityTokenClient;

public class EPEventTokenGranted extends AbstractEvent {
	private EntityTokenClient token;
	public EntityTokenClient getToken() {
		return token;
	}
	public void setToken(EntityTokenClient token) {
		this.token = token;
	}
	
	public EPEventTokenGranted(int id) {
		super(id);
	}
}